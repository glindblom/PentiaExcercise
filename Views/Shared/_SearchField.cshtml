@model SearchFieldViewModel
<form asp-controller="@Model.Controller" asp-action="@Model.Action" class="form form-horizontal">
    <div class="col-md-6">
        <input type="text" class="form-control" name="searchString" placeholder="@Model.Placeholder">
    </div>
    <input type="submit" value="Search" class="btn btn-default" />
</form>